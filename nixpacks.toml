[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
dependsOn = ["setup"]
cmds = ["python -m pip install --no-cache-dir -r requirements_railway.txt"]

[start]
cmd = "gunicorn app.app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1"
